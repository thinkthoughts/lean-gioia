# Checkpoint 19 — mixed normal-ordered syntax and vacuum bridge

The source basis uses normal-ordered terms

`s†_{j1} ... s†_{jn} s_{k1} ... s_{km}`.

Checkpoint 19 introduces explicit creator and annihilator lists and proves:

`annihilators ≠ [] → term |0̄⟩ = 0`.

This removes the abstract `VacuumAnnihilating` assumption from the
Checkpoint 18 vacuum-eigenstate theorem for mixed normal-ordered terms.

Run:

```bash
lake build
```
